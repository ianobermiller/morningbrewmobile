var feedparser = require('./')
  , request = require('request');

var urls = [
  { uri: 'http://yabfog.com/blog/feed' },
  'http://scripting.com/rss.xml',
  'http://cyber.law.harvard.edu/rss/examples/rss2sample.xml',
  'http://cyber.law.harvard.edu/rss/examples/sampleRss092.xml',
  'http://cyber.law.harvard.edu/rss/examples/sampleRss091.xml'
];

var parser = new feedparser({ normalize: false });
// feedparser.parseStream(request(url))

// urls.forEach(function (url) {
//   request(url).pipe(new feedparser())
//     .on('meta', function (meta) {
//       console.log(meta.title);
//     });
// });
request(urls[0], function (err, response, body) {
  parser.on('meta', console.log).end(body);
})